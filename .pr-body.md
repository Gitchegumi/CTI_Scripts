## Problem

Dependabot security update job #1412565169 (run #39) failed with:
> "Dependabot can't update vulnerable dependencies for projects without a lockfile or pinned version requirement as the currently installed version of pytest isn't known."

## Root Cause

- `.github/dependabot.yml` pointed `pip` ecosystem to `/`, but the Python manifest (`pyproject.toml`) lives in `/src/`
- The root `requirements.txt` doesn't include `pytest` (it's a dev dependency in `src/pyproject.toml`)
- No `npm` ecosystem was configured for the Next.js frontend

## Changes

- Move `pip` ecosystem from `/` to `/src` where `pyproject.toml` lives
- Add `npm` ecosystem for Next.js dependencies at root
- Set reasonable PR limits (5 for pip, 10 for npm) to avoid noise

## References

- Failed run: https://github.com/Gitchegumi/CTI_Scripts/actions/runs/27522857687
- Fixes Dependabot job 1412565169

---
*Auto-generated by CodeGumi agent*
